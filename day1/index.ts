import { calculateSlidingWindowIncreaseDepthCount } from './calculate-depth'
import { input } from './input'

export const run = () => {
  // console.log(calculateIncreaseDepthCount(input))
  console.log(calculateSlidingWindowIncreaseDepthCount(input))
}
