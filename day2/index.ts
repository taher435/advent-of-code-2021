import { navigate, navigateWithAim } from './navigate'
import { directions } from './input'

export const run = () => {
  // console.log(navigate(directions))
  console.log(navigateWithAim(directions))
}
